import { Navbar } from "../../components/navbar/Navbar";
import "./Header.css";

const Header = () => {
  return (
    <section id="header">
      <Navbar />
    </section>
  );
};

export default Header;
